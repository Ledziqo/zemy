{
  "name": "zemtab-frontend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build:css && npm run build:alpine",
    "build:css": "tailwindcss -c tailwind.config.cjs -i resources/css/app.css -o public/assets/app.css --minify",
    "build:alpine": "node scripts/vendor-alpine.cjs",
    "watch": "npm run build:alpine && tailwindcss -c tailwind.config.cjs -i resources/css/app.css -o public/assets/app.css --watch",
    "check:assets": "node scripts/check-assets.cjs"
  },
  "devDependencies": {
    "alpinejs": "3.14.9",
    "tailwindcss": "3.4.17"
  }
}
