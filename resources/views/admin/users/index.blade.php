@extends('layouts.dashboard', ['heading' => 'Users'])

@section('content')
<form method="post" action="{{ route('admin.users.store') }}" class="mb-6 grid gap-3 rounded-md border border-zem-border bg-zem-card p-4 md:grid-cols-5">@csrf<input name="name" required placeholder="Name" class="rounded-md border border-zem-border bg-zem-bg px-3 py-2"><input name="email" type="email" required placeholder="Email" class="rounded-md border border-zem-border bg-zem-bg px-3 py-2"><input name="password" required placeholder="Password" class="rounded-md border border-zem-border bg-zem-bg px-3 py-2"><select name="role" class="rounded-md border border-zem-border bg-zem-bg px-3 py-2"><option>restaurant_owner</option><option>staff</option><option>admin</option></select><select name="restaurant_id" class="rounded-md border border-zem-border bg-zem-bg px-3 py-2"><option value="">No restaurant</option>@foreach($restaurants as $restaurant)<option value="{{ $restaurant->id }}">{{ $restaurant->name }}</option>@endforeach</select><button class="rounded-md bg-zem-gold px-4 py-2 font-bold text-white md:col-span-5">Create user</button></form>
<div class="grid gap-3">@foreach($users as $user)<form method="post" action="{{ route('admin.users.update', $user) }}" class="grid gap-3 rounded-md border border-zem-border bg-zem-card p-4 md:grid-cols-4">@csrf @method('PATCH')<input name="name" value="{{ $user->name }}" class="rounded-md border border-zem-border bg-zem-bg px-3 py-2"><select name="role" class="rounded-md border border-zem-border bg-zem-bg px-3 py-2">@foreach(['admin','restaurant_owner','staff'] as $role)<option value="{{ $role }}" @selected($user->role===$role)>{{ $role }}</option>@endforeach</select><select name="restaurant_id" class="rounded-md border border-zem-border bg-zem-bg px-3 py-2"><option value="">No restaurant</option>@foreach($restaurants as $restaurant)<option value="{{ $restaurant->id }}" @selected($user->restaurant_id===$restaurant->id)>{{ $restaurant->name }}</option>@endforeach</select><button class="rounded-md bg-zem-gold px-4 py-2 font-bold text-white">Save</button><p class="text-sm text-zem-muted md:col-span-4">{{ $user->email }} · {{ $user->restaurant->name ?? 'Platform' }}</p></form>@endforeach</div>
@endsection
